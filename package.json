{
  "name": "laesko site",
  "version": "0.0.1",
  "description": "Lasko shoemakers site",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
